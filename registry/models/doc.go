// Package models is the durable model-definition store.
//
// A model is an inference configuration independent of roles: provider/runtime,
// identifier/version, capabilities, context limits, tool support, locality,
// availability, cost, latency, and measured performance. The specification
// types and their contracts land with contracts v2; this package is a reserved
// architectural boundary, not an implementation.
package models
