# registry/capabilities

> Project history for the `registry/capabilities` submodule of [aa-registry](./README.md).

**Responsibility** — The closed execution-capability vocabulary (`Known`): the
authority a work package may be granted under an execution contract. Grants and
denials are decided at runtime by the kernel.

**Source** — [`registry/capabilities/`](../../../registry/capabilities/)

**Parent module** — [aa-registry](./README.md) · [docs/modules](../README.md)
