# kernel/allocator/planner

> Project history for the `kernel/allocator/planner` submodule of [aa-kernel](./README.md).

**Responsibility** — The kernel allocator's planning stage: it models work packages and their dependency graph and computes deterministic dispatch readiness. It is the Planner that produces the Work Plan; it does not select roles, models, or compute.

**Source** — [`kernel/allocator/planner/`](../../../kernel/allocator/planner/)

**Parent module** — [aa-kernel](./README.md) · [docs/modules](../README.md)
