# kernel/orchestrator

> Project history for the `kernel/orchestrator` submodule of [aa-kernel](./README.md).

**Responsibility** — Package orchestrator runs aa-kernel's deterministic supervised control cycle: ready -> select worker -> build contract -> compile context -> lease -> run -> intake -> gates -> accept -> telemetry.

**Source** — [`kernel/orchestrator/`](../../../kernel/orchestrator/)

**Parent module** — [aa-kernel](./README.md) · [docs/modules](../README.md)
