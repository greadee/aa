# kernel/allocator/model_allocator

> Project history for the `kernel/allocator/model_allocator` submodule of [aa-kernel](./README.md).

**Responsibility** — Reserved boundary for model allocation: selecting a model
from the model registry for each required role/worker assignment, owning model
identity selection (never worker count, concurrency, or compute budgets). No
behavior yet; the refactor reserves the boundary.

**Source** — [`kernel/allocator/model_allocator/`](../../../kernel/allocator/model_allocator/)

**Parent module** — [aa-kernel](./README.md) · [docs/modules](../README.md)
