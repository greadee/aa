# kernel/registry

> Project history for the `kernel/registry` submodule of [aa-kernel](./README.md).

**Responsibility** — Package registry holds durable roles and trades and the worker instances that can perform work, and selects workers deterministically.

**Source** — [`kernel/registry/`](../../../kernel/registry/)

**Parent module** — [aa-kernel](./README.md) · [docs/modules](../README.md)
