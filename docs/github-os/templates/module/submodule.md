# {module}/{submodule}

> Project history for the `{module}/{submodule}` submodule of the `{module}` module.
> Part of [docs/modules](../../../modules/README.md).

**Responsibility** — One sentence: what this submodule owns.

**Source** — `{module}/{submodule}/`

**Parent module** — `{module}/README.md`
