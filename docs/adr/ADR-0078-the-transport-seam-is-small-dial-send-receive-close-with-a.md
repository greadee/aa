# ADR-0078 — The `Transport` seam is small (`Dial`/`Send`/`Receive`/`Close`) with a deterministic in-memory fake

| Field | Value |
|---|---|
| **Status** | Accepted |
| **Legacy id** | `ADR-P6-003` |
| **Source** | [docs/phases/ph6-sync/plan.md](../phases/ph6-sync/plan.md) |
| **Scope** | phase · sync |

## Context

Backfilled from the original decision record for **phase 6 (sync)**.
Only the decision and its stated rationale were recorded at the time; alternatives
and consequences were not.

## Decision

The `Transport` seam is small (`Dial`/`Send`/`Receive`/`Close`) with a deterministic in-memory fake

## Rationale

Provider independence and offline end-to-end tests

## Alternatives and consequences

Not recorded in the original decision record. See the [source](../phases/ph6-sync/plan.md).

## Related

- Legacy id `ADR-P6-003`
- [ADR index](./README.md)
